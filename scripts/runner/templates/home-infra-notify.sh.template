#!/bin/sh
set -e
# Runs as deploy-bot (no sudo): sending mail needs no privilege, and the
# python below is a fixed root-owned path, so there is nothing here for a
# sudoers rule to scope. Credentials arrive as environment variables from
# the workflow step.
exec python3 "@@NOTIFY_PY@@"
